// Function to run the mod code
function runMod() {
    // Generate a random number between 1 and 10
    let randomNumber = Math.floor(Math.random() * 10) + 1;
  
    // Display the number in an alert box
    alert("The random number is: " + randomNumber);
  }
  
  // Example of how to use it in your mod script
  runMod();  // This will execute the mod code for the first time
  